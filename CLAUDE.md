# CLAUDE.md — Exhs project rules

Project-specific guidance for AI agents working on this codebase. Framework-level rules (Ash, Phoenix) live in the Claude Code skills at `.claude/skills/ash-framework/` and `.claude/skills/phoenix-framework/`, generated by `mix usage_rules.sync`. Do not duplicate framework rules here.

## General guidance

### Ash First
Always use Ash concepts, almost never Ecto concepts directly. Think hard about the "Ash way" to do things. If you don't know, look in the `ash-framework` skill and the rules & docs of associated packages.

### Code generation
Start with generators wherever possible. They provide a starting point for your code and can be modified if needed.

### Logs & tests
When you're done executing code, try to compile the code, and check the logs or run any applicable tests to see what effect your changes have had.

## Tidewave

Use Tidewave MCP tools — they let you interrogate the running application in various useful ways.

- Never attempt to start or stop a Phoenix application. Tidewave tools work by being connected to the running application; starting or stopping it can cause issues.
- Use the `project_eval` tool to execute code in the running instance. Eval `h Module.fun` to get documentation for a module or function.
- Always use `search_package_docs` to find relevant documentation before beginning work.

## AshAI

- Use `list_generators` to list available generators.
- Always prefer generators as a basis for code generation, then modify afterwards.
- If you have to run generator tasks, pass `--yes`.

## Project orientation

- App module: `Exhs`. Source: `lib/exhs/`. Web layer: `lib/exhs_web/`.
- Scope of work and roadmap: `tasks/00-plan.md`. Each task has its own file. Do not invent scope outside what a task file describes.
- Decisions live inside task files under "Decided" sections. Open questions are tracked in task files and `tasks/00-plan.md`.

## Project conventions

### Multitenancy
Most resources are tenant-scoped to a `Forening`. Use Ash's attribute multitenancy strategy on `:forening_id`. Pass tenant + actor via `Exhs.Scope` (`%Exhs.Scope{tenant: forening_id, actor: user}`) to every action. Code interface calls take `scope: socket.assigns.scope` from LiveViews; inside action hooks use the `context` parameter as the scope.

### Code interfaces are the only entry point
- Define code interfaces on the domain (`define :register_user, action: :register`).
- Call code interfaces from LiveViews and controllers — **never** call `Ash.create!/2` or similar directly from web modules.
- **Never** call `Repo` directly outside of explicit infrastructure code.

### HTTP
Use `Req` for outbound HTTP requests. Do not add `httpoison`, `tesla`, or `httpc`.

### Mix
- Run `mix format` and `mix credo --strict` before considering any change done.
- Use `mix precommit` when finishing a chunk of work; it runs format, credo, compile, tests, and skills drift check.
- Read `mix help <task>` before using unfamiliar tasks.
- `mix deps.clean --all` is almost never needed.
- After adding ash_* or phoenix_* deps, re-run `mix usage_rules.sync --yes` to update skills.

### Migrations
After resource changes, run `mix ash.codegen --dev` iteratively, then a final `mix ash.codegen <feature_name>` at the end of a session to consolidate.

## Code quality

`mix credo --strict` runs the ExSlop plugin (30+ AI-slop checks) alongside standard Credo. It flags patterns like blanket rescues, narrator-style comments/docs, N+1 Ecto queries, redundant Enum chains, and identity passthroughs. Run `mix precommit` before finishing a chunk of work — it includes credo.

## Do not

- Do not use raw Ecto in domain code (use Ash).
- Do not use `String.to_atom/1` on user input (memory leak risk).
- Do not use DaisyUI components — write Tailwind components by hand. See Task 14 for the design system.
- Do not set `require_atomic? false` without a justified reason; prefer atomic changes.
- Do not write inline `<script>` tags in templates. Use colocated JS hooks (`:type={Phoenix.LiveView.ColocatedHook}`) or external hooks under `assets/js/`.
- Do not reference external vendor scripts/CSS via `src`/`href` in layouts. Bundle them through `app.js` / `app.css`.
- Do not use `@apply` when writing raw CSS.
- Do not nest multiple modules in the same file.
- Do not assign inside `if`/`case`/`cond` blocks expecting to mutate; rebind the result of the expression.

## Phoenix 1.8 specifics for this codebase

- Always begin LiveView templates with `<Layouts.app flash={@flash} ...>` (the `ExhsWeb.Layouts` module is aliased in `exhs_web.ex`).
- `<.flash_group>` lives only in `layouts.ex` — do not call it elsewhere.
- Use `<.icon name="hero-x-mark" class="w-5 h-5"/>` from `core_components.ex`. Never import `Heroicons` modules.
- Use the imported `<.input>` from `core_components.ex` for form inputs.
- If you encounter a missing `current_scope` assign error, move routes into the proper `live_session` and pass `current_scope` to `<Layouts.app>`.

## Tailwind v4 in this codebase

Tailwind v4 — no `tailwind.config.js`. CSS uses this import syntax in `assets/css/app.css`:

```css
@import "tailwindcss" source(none);
@source "../css";
@source "../js";
@source "../../lib/exhs_web";
```

Maintain this syntax. Do not introduce `tailwind.config.js`.

## Useful skills

When relevant, lean on these skills:
- `ash-framework` — Ash and ash_* usage rules
- `phoenix-framework` — Phoenix and phoenix_* usage rules
- `simplify` — review for reuse and quality after writing code
- `review` — review a pull request
- `security-review` — security pass on the current branch

## Useful mix tasks

- `mix usage_rules.docs Enum.zip` — search docs for a function
- `mix usage_rules.search_docs "making requests" -p req` — search docs for a phrase, optionally scoped to packages
- `mix ash.codegen --dev` — interim migration
- `mix ash.codegen <feature_name>` — final named migration
- `mix ash.migrate` — apply migrations
- `mix precommit` — full pre-commit check
