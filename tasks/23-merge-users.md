# Task 23 — Merge duplicate user accounts

## Goal
Allow a superadmin to merge two user accounts into one when a person has accidentally created multiple accounts. All data from the source account (memberships, event registrations, payments, orders, etc.) is transferred to the target account, and the source account is deleted.
